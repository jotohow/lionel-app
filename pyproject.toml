[project]
name = "lionel-app"
version = "0.1.0"
description = "Streamlit front end for the Lionel FPL system"
requires-python = ">=3.11"
dependencies = [
    "streamlit>=1.36",
    "pandas>=2.2",
    "plotly>=5.24",
    "duckdb>=1.4",
]

[tool.uv]
package = false
